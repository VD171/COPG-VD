name: Build inotify-tools for Android

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  build:
    runs-on: ubuntu-24.04
    strategy:
      matrix:
        target: [armv7a-linux-androideabi, aarch64-linux-android]  # ARMv7 + ARMv8
        api_level: [21]  # Android 5.0+
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Android SDK and NDK
        run: |
          echo "ANDROID_HOME=$HOME/android-sdk" >> $GITHUB_ENV
          mkdir -p $ANDROID_HOME/cmdline-tools
          wget https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O cmdline-tools.zip
          ls -la cmdline-tools.zip || echo "Download failed!"
          unzip -q cmdline-tools.zip -d $ANDROID_HOME/cmdline-tools
          ls -la $ANDROID_HOME/cmdline-tools || echo "Unzip failed!"
          if [ -d "$ANDROID_HOME/cmdline-tools/cmdline-tools" ]; then
            mv $ANDROID_HOME/cmdline-tools/cmdline-tools $ANDROID_HOME/cmdline-tools/latest
          elif [ -d "$ANDROID_HOME/cmdline-tools/tools" ]; then
            mv $ANDROID_HOME/cmdline-tools/tools $ANDROID_HOME/cmdline-tools/latest
          else
            echo "Unexpected directory structure after unzip:"
            ls -la $ANDROID_HOME/cmdline-tools
            exit 1
          fi
          ls -la $ANDROID_HOME/cmdline-tools/latest/bin || echo "latest/bin directory not found!"
          if [ ! -f "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" ]; then
            echo "sdkmanager not found!"
            exit 1
          fi
          echo "$ANDROID_HOME/cmdline-tools/latest/bin" >> $GITHUB_PATH
          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "ndk;27.0.12077973"
          echo "NDK_PATH=$ANDROID_HOME/ndk/27.0.12077973" >> $GITHUB_ENV

      - name: Accept Android SDK licenses
        run: |
          JAVA_OPTS="--add-modules java.xml.bind" yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses || echo "License acceptance failed!"

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential autoconf automake libtool file

      - name: Clone inotify-tools source
        run: |
          git clone https://github.com/inotify-tools/inotify-tools.git --depth=1
          cd inotify-tools
          

      - name: Build inotify-tools
        env:
          NDK: ${{ env.NDK_PATH }}
          TARGET: ${{ matrix.target }}
          API: ${{ matrix.api_level }}
        run: |
          cd inotify-tools
          export TOOLCHAIN=$NDK/toolchains/llvm/prebuilt/linux-x86_64
          export CC=$TOOLCHAIN/bin/${TARGET}${API}-clang
          export CXX=$TOOLCHAIN/bin/${TARGET}${API}-clang++
          export AR=$TOOLCHAIN/bin/llvm-ar
          export LD=$TOOLCHAIN/bin/ld
          export RANLIB=$TOOLCHAIN/bin/llvm-ranlib
          export STRIP=$TOOLCHAIN/bin/llvm-strip
          echo "Using CC: $CC"
          $CC --version
          ./autogen.sh
          ./configure \
            --host="$TARGET" \
            --prefix=/data/local/tmp \
            --disable-maintainer-mode \
            --enable-static \
            --disable-shared \
            --disable-doxygen \
            --with-cxx=none \
            CC="$CC" \
            CXX="$CXX" \
            AR="$AR" \
            LD="$LD" \
            RANLIB="$RANLIB" \
            STRIP="$STRIP" \
            CFLAGS="-O2 -fPIE" \
            LDFLAGS="-static -static-libgcc -pie"
          make -j$(nproc)
          file ./src/inotifywait  # Debug: Check binary type
          ldd ./src/inotifywait || echo "ldd: Binary is statically linked"
          $STRIP ./src/inotifywait
          mv ./src/inotifywait "../inotifywait-$TARGET"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: inotifywait-android-${{ matrix.target }}
          path: inotifywait-${{ matrix.target }}
